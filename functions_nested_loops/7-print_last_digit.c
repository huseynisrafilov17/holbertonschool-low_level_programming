#include "main.h"
/**
 * print_last_digit - checks for lowercase character.
 * @a:  int.
 * Return: void.
 */
int print_last_digit(int a)
{
	int b;

	if (a < 0)
	{
		a = -1 * a;
	}
	a % 10;
	_putchar(b + '0');
	return (b);
}
